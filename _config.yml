# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: IT Client Platform Engineer
email: austinculter@gmail.com
description: >- # this means to ignore newlines until "baseurl:"
  "Calmer than you are, dude..."
baseurl: "" # the subpath of your site, e.g. /blog
url: "http://austinculter.com" # the base hostname & protocol for your site, e.g. http://example.com
twitter_username: ChefWithGusto
github_username:  ChefAustin
author: Austin Culter

# Build settings
markdown: kramdown
kramdown:
  input: GFM
syntax_highlighter: rouge
theme: minima

highlighter: rouge
sass:
sass_dir: _sass

# Custom plugins
plugins:
  - jemoji
  - jekyll-feed
  - jekyll-mentions
  - jekyll-seo-tag
  - jekyll-gist
